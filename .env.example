# Copy to .env for `docker compose up`. The stack runs without an OpenAI key
# (AI features degrade gracefully to "AI disabled").
JWT_SECRET=change-me-to-a-long-random-string
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
VITE_API_BASE=http://localhost:8000
